# Prompt 03 — Demo UI

Use the master prompt and project rules.

Implement only the demo UI:
- dashboard
- rooms and devices view
- event log
- command console
- clarification panel
- debug trace panel
- demo controls

Requirements:
- React + TypeScript + Vite
- local-only setup
- UI must be engineering-friendly, not decorative
- text input is mandatory
- current state must come from backend and not become a second source of truth

After implementation, list the created files and explain how to run the UI.