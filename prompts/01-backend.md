# Prompt 01 — Backend baseline

Use the master prompt and project rules.

Implement only the backend MVP baseline:
- FastAPI app
- app structure
- Pydantic models
- GET /api/health
- GET /api/state/house
- GET /api/events
- POST /api/intents/execute
- basic scenario_engine skeleton
- basic event_log service
- tests for the baseline endpoints

Requirements:
- real working code, not pseudocode
- typed models
- clear module boundaries
- no unnecessary abstractions
- do not implement full voice integration yet

After implementation, list the files created or modified and explain what still needs to be built.